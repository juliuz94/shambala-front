type Routes = {
  VIDEOS: string;
  COMMENT: string;
  COMMENTS: string;
};

const ROUTES: Routes = {
  VIDEOS: 'video',
  COMMENT: 'video/comment',
  COMMENTS: 'video/comments'
};

export default ROUTES;